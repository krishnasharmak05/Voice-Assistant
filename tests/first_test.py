from voice_assistant.src.helper.classes import Voice_Assistant

def test_first() -> None: 
    # Writing an assertion that always passes because the code is not yet written completely.
    assert 1 == 1